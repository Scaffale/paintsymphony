<% unless @phase.picture %>
	<%= audio_tag @phase.music.sound, controls:true, style:"width:300px;", preload:'metadata', type: "audio/#{@phase.music.sound.extension}" %>
<% else %>
	<%= image_tag @phase.picture.image, class: 'img-responsive' %>
<% end %>

