<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Paintsymphony" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Paintsymphony" %>">
    <script src="https://www.atlasestateagents.co.uk/javascript/tether.min.js"></script><!-- Tether for Bootstrap --> 
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <% if (current_page?(data_settings_path) || current_page?(sessions_path) || current_page?(data_manage_path) || current_page?(pictures_path) || current_page?(musics_path)) || current_page?(words_path) %>      
        <%= render 'layouts/navigation' %>
      <% end %>
        
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
  </body>
</html>
